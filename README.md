# Constra Template

A static HTML/CSS/JS frontend template for construction and industrial companies.

## Usage

Simply edit the HTML, CSS, and JS files to customize the template. You can open any HTML file (such as `index.html`) in a browser to preview the site.
